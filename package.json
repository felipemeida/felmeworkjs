{
  "name": "crawler",
  "version": "1.0.0",
  "description": "",
  "main": "examples/index.js",
  "scripts": {
    "server": "export NODE_ENV=test; nodemon server.js",
    "server-prod": "export NODE_ENV=production; nodemon server.js",
    "test": "jest",
    "migrate-test": "export NODE_ENV=test;npx sequelize-cli db:migrate",
    "migrate-prod": "export NODE_ENV=production;npx sequelize-cli db:migrate",
    "seed-test": "export NODE_ENV=test;npx sequelize-cli db:seed:all",
    "seed-prod": "export NODE_ENV=production;npx sequelize-cli db:seed:all"
  },
  "author": "Felipe de Almeida",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "clipboardy": "^2.3.0",
    "config": "^3.3.6",
    "ejs": "^3.1.6",
    "express": "^4.17.1",
    "mysql2": "^2.3.0",
    "puppeteer": "^10.1.0",
    "sequelize": "^6.6.5"
  },
  "devDependencies": {
    "jest": "^27.0.6",
    "nodemon": "^2.0.12",
    "prettier": "2.3.2",
    "sequelize-cli": "^6.2.0"
  }
}
